from datetime import timedelta
DEFUALT_CELL_INTERVAL = timedelta(minutes=1) #timedelta(hours=1)
DEFAULT_TASK_MARGIN = timedelta(minutes=15)
DEFAULT_MINIMUM_CHUNK_SIZE = timedelta(minutes=15)
DEFAULT_SUBMISSION_MARGIN = timedelta(hours=4)