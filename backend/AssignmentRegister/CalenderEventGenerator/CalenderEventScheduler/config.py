from datetime import timedelta
CELL_INTERVAL = timedelta(minutes=1) #timedelta(hours=1)
TASK_MARGIN = timedelta(minutes=15)
MINIMUM_CHUNK_SIZE = timedelta(minutes=15)
SUBMISSION_MARGIN = timedelta(hours=4)